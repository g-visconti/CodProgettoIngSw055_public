version: '3.8'

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: dietiestates-app
    environment:
      DB_HOST: database-1.cshockuiujqi.us-east-1.rds.amazonaws.com
      DB_NAME: DietiEstates25DB
      AWS_REGION: us-east-1
    networks:
      - app-network
    restart: unless-stopped

networks:
  app-network:
    driver: bridge